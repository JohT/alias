/**
 * This package contains the implementation of the boundary API for messaging using axon. (see "hexagonal architecture")
 * <p>
 * The domain model shouldn't use anything in this package directly. <br>
 * Both should only depend on the abstraction, which is the messaging boundary API.
 */
package org.alias.axon.serializer.example.messaging.axon.configuration;

;
